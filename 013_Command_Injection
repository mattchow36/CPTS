OS commands
Code
SQL
HTML scripts
除了上述类型之外，还有许多其他类型的注入，例如 LDAP injection、NoSQL Injection、HTTP Header Injection、XPath Injection、IMAP Injection、ORM Injection 等


system in PHP
exec in NodeJS


If only has front end control, use Burp repeater to bypass

绕过空格:
  tab
  ${IFS}            默认是空格或tab

