HTTP 动词篡改 (HTTP Verb Tampering                                       1
不安全的直接对象引用 (Insecure Direct Object References, IDOR)            2
XML 外部实体注入 (XML External Entity (XXE) Injection)                   3


1:
GET

HEAD  -  As GET request, but no response body.      通常应考虑禁用/拒绝所有 HEAD 请求

POST  -  Add.

PUT   -  Update.

DELETE

CONNECT
The CONNECT method establishes a tunnel to the server identified by the target resource.

OPTIONS  -  Show the communication option.            curl -i -X OPTIONS http://SERVER_IP:PORT/

TRACE
The TRACE method performs a message loop-back test along the path to the target resource.

PATCH  -  Partial modifications.

单纯改上面这些请求方法，也可能绕过

在 PHP 中，$_REQUEST 是一个超全局变量，它默认包含了 $_GET、$_POST 和 $_COOKIE 的集合

它位于 /admin/reset.php，要么是 /admin 目录仅限于经过身份认证的用户访问，要么只有 /admin/reset.php 页面受限
由于该页面使用 GET 请求，我们可以发送一个 POST 请求


2:
file; cp /flag.txt ./            换路径

base64 a no. then pass and get:
  for i in {1..20}; do
      for hash in $(echo -n $i | base64 -w 0); do
          curl -sOJ "http://STMIP:STMPO/download.php?contract=$hash"
      done
  done


Cookie: role=employee
  po vuls

一种攻击类型是修改用户的电子邮件地址，然后请求密码重置链接
另一种潜在的攻击是在用户可编辑字段中放置跨站脚本 (Cross-Site Scripting, XSS) 载荷‘


Defensive:
  Apply RBAC
  JS code:
    match /api/profile/{userId} {
      allow read, write: if user.isAuth == true
      && (user.uid == userId || user.roles == 'admin');
  }

绝不应在前端计算哈希值


3:
XML 不侧重于显示数据，而主要用于存储文档数据和表示数据结构

Declaration	通常是 XML 文档的第一行，定义解析时要使用的 XML 版本和编码。	<?xml version="1.0" encoding="UTF-8"?>
DTD(XML Document Type Definition) XML 文档类型定义   紧跟在第一行的 XML 声明 之后，自定义标签结构
      <!DOCTYPE email [
      <!ENTITY company SYSTEM "http://localhost/company.txt">
      <!ENTITY signature SYSTEM "file:///var/www/html/signature.txt">
    ]>
      使用 SYSTEM 关键字在 XML 文档中引用：
            <!DOCTYPE email SYSTEM "email.dtd">
            <!DOCTYPE email SYSTEM "http://inlanefreight.com/email.dtd">
Attribute	存储在标签中的任何元素的可选规范，可供 XML 解析器使用。	version="1.0"/encoding="UTF-8"
Tag	XML 文档的键，通常用 (< >) 字符包裹。	<date>
Entity	XML 变量，通常用 (& ;) 字符包裹。	&lt;
Element	根元素或其任何子元素，其值存储在开始标签和结束标签之间。	<date>01-01-2022</date>
Comments注释  <!-- 和 --> 

