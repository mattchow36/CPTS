Web Shell 必须使用运行 Web 服务器的相同编程语言编写，
因为它运行特定于平台的函数和命令来在后端服务器上执行系统命令，
这使得 Web Shell 成为非跨平台的脚本。
因此，第一步将是识别 Web 应用程序运行的是什么语言。


Check website stack:
  https://www.wappalyzer.com/


Front end controls:
  Use Burp to modify
  OR
  Change front end code


Back end controls:
  Black list:
    Use Burp Intruder to fuff  (fuff the para of 'filename')
  
    untick url encoding, keep .
  
    Send 200 request from Intruder to Repeater

  White list:
    use double extention (e.g. ending with jpg if jpg in white list      *.php.jpg)
    BUT
    if (!preg_match('/^.*\.(jpg|jpeg|png|gif)$/', $fileName))      $ match the last extention, so can not bypass

    shell.php%00.jpg 适用于版本为 5.X 或更早的 PHP 服务器，因为它会导致 PHP Web 服务器在 (%00) 之后结束文件名，并将其存储为 (shell.php)，同时仍然通过白名单。
    shell.aspx:.jpg，这也应该会将文件写为 shell.aspx
  

Extention wordlist:
  https://github.com/swisskyrepo/PayloadsAllTheThings/blob/master/Upload%20Insecure%20Files/Extension%20PHP/extensions.lst

Content-type wordlist:
  https://github.com/danielmiessler/SecLists/blob/master/Discovery/Web-Content/web-all-content-types.txt

Bash hand make wordlist:
  for char in '%20' '%0a' '%00' '%0d0a' '/' '.\\' '.' '…' ':'; do
      for ext in '.php' '.phps'; do
          echo "shell$char$ext.jpg" >> wordlist.txt
          echo "shell$ext$char.jpg" >> wordlist.txt
          echo "shell.jpg$char$ext" >> wordlist.txt
          echo "shell.jpg$ext$char" >> wordlist.txt
      done
  done

