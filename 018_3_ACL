ACL 有两种类型：
自主访问控制列表 (Discretionary Access Control List, DACL) - 定义了哪些安全主体被授予或拒绝访问某个对象。
  DACL 由允许或拒绝访问的 ACE 组成。
  当有人尝试访问一个对象时，系统将检查 DACL 以确定所允许的访问级别。
  如果一个对象不存在 DACL，所有试图访问该对象的人都将被授予完全权限。
  如果 DACL 存在，但没有任何 ACE 条目指定特定的安全设置，系统将拒绝所有试图访问它的用户、组或进程的访问。
系统访问控制列表 (System Access Control Lists, SACL) - 允许管理员记录对受保护对象的访问尝试。

ACL 中的设置本身被称为 访问控制条目 (Access Control Entries, ACE)。每个 ACE 都对应一个用户、组或进程（也称为安全主体 (security principals)）


ACE 类型有三种：
拒绝访问 ACE	在 DACL 中使用，表示明确拒绝某个用户或组访问一个对象
允许访问 ACE	在 DACL 中使用，表示明确授予某个用户或组访问一个对象
系统审核 ACE	在 SACL 中使用，当用户或组尝试访问一个对象时生成审核日志，记录访问是否被授予以及发生了哪种类型的访问


每个 ACE 由四个部分组成：
有权访问该对象的用户/组的安全标识符 (security identifier, SID)（或图形化显示的主体名称）
表示 ACE 类型的标志（拒绝访问、允许访问或系统审核 ACE）
一组标志，指定子容器/对象是否可以从主对象或父对象继承给定的 ACE 条目
一个访问掩码 (access mask)，它是一个 32 位的值，定义了授予对象的权限


ACE无法被漏洞扫描工具检测到，并且通常多年未被检查，尤其是在大型和复杂的环境中


可使用如 BloodHound 之类的工具进行枚举（和可视化），并且通过 PowerView 等工具进行滥用：
ForceChangePassword，可通过 Set-DomainUserPassword 滥用
Add Members，可通过 Add-DomainGroupMember 滥用
GenericAll，可通过 Set-DomainUserPassword 或 Add-DomainGroupMember 滥用
GenericWrite，可通过 Set-DomainObject 滥用
WriteOwner，可通过 Set-DomainObjectOwner 滥用
WriteDACL，可通过 Add-DomainObjectACL 滥用
AllExtendedRights，可通过 Set-DomainUserPassword 或 Add-DomainGroupMember 滥用
AddSelf，可通过 Add-DomainGroupMember 滥用


以下使用PowerView，Bloodhound更容易完成
C:\htb> Import-Module .\PowerView.ps1
C:\htb> $sid = Convert-NameToSid wley      设置sid参数
C:\htb> Get-DomainObjectACL -Identity * | ? {$_.SecurityIdentifier -eq $sid}      只枚举wley
返回      ObjectAceType : 00299570-246d-11d0-a768-00aa006e0529      
用google查这个GUID，对应ForceChangePassword权限
OR
反向搜索
C:\htb> $guid= "00299570-246d-11d0-a768-00aa006e0529"
C:\htb> Get-ADObject -SearchBase "CN=Extended-Rights,$((Get-ADRootDSE).ConfigurationNamingContext)" -Filter {ObjectClass -like 'ControlAccessRight'} -Properties * |Select Name,DisplayName,DistinguishedName,rightsGuid| ?{$_.rightsGuid -eq $guid} | fl
OR
直接加  -ResolveGUIDs
C:\htb> Get-DomainObjectACL -ResolveGUIDs -Identity * | ? {$_.SecurityIdentifier -eq $sid}
另外一个方式
C:\htb> Get-ADUser -Filter * | Select-Object -ExpandProperty SamAccountName > ad_users.txt      创建所有域用户的列表
C:\htb> foreach($line in [System.IO.File]::ReadLines("C:\Users\htb-student\Desktop\ad_users.txt")) 
        {get-acl  "AD:\$(Get-ADUser $line)" |             检索每个域用户的 ACL 信息
        Select-Object Path -ExpandProperty Access |       只选择 Access 属性
        Where-Object {$_.IdentityReference -match 'INLANEFREIGHT\\wley'}}      设置为wley
发现用户wley对用户damundsen有ForceChangePassword权限
继续枚举damundsen
C:\htb> $sid2 = Convert-NameToSid damundsen
C:\htb> Get-DomainObjectACL -ResolveGUIDs -Identity * | ? {$_.SecurityIdentifier -eq $sid2} -Verbose
发现用户 damundsen 对 Help Desk Level 1 组拥有 GenericWrite 权限，除了其他功能外，可以将任何用户（或自己）添加到该组，并继承该组所拥有的任何权限
C:\htb> Get-DomainGroup -Identity "Help Desk Level 1" | select memberof      调查 Help Desk Level 1 组

回顾一下进展：
控制了用户 wley，其哈希值是我们在本模块（评估）前面使用 Responder 获取的，并使用 Hashcat 离线破解得到了明文密码值。
举了用户 wley 所控制的对象，发现我们可以强制更改用户 damundsen 的密码。
发现，damundsen 用户可以使用 GenericWrite 权限将成员添加到 Help Desk Level 1 组中。
Help Desk Level 1 组嵌套在 Information Technology 组中，这使得该组的成员可以获得分配给 Information Technology 组的任何权限。
现在看看 Information Technology 的成员是否能做些什么。
再次使用 Get-DomainObjectACL 进行搜索
C:\htb> $itgroupsid = Convert-NameToSid "Information Technology"
C:\htb> Get-DomainObjectACL -ResolveGUIDs -Identity * | ? {$_.SecurityIdentifier -eq $itgroupsid} -Verbose
结果显示 Information Technology 组的成员对用户 adunn 拥有 GenericAll 权限，这意味着可以：
            修改组成员身份
            强制更改密码
            执行有针对性的 Kerberoasting 攻击，并尝试破解该用户的密码（如果密码较弱）
再看adunn用户
C:\htb> $adunnsid = Convert-NameToSid adunn
C:\htb> Get-DomainObjectACL -ResolveGUIDs -Identity * | ? {$_.SecurityIdentifier -eq $adunnsid} -Verbose
结果显示 adunn 用户对域对象拥有 DS-Replication-Get-Changes 和 DS-Replication-Get-Changes-In-Filtered-Set 权限，可以利用该用户执行 DCSync 攻击


如果 PowerView 被导入，cmdlet 将会导致错误，可能需要从一个新的 PowerShell 会话中运行它
C:\Tools> Get-DomainObjectAcl -ResolveGUIDs -Identity "GPO Management" | ? {$_.SecurityIdentifier -eq $sid}      -Identity设置只看GPO Management组，与上面无关


开始abuse
回顾一下目前所处的位置以及想要达成的目标
已经控制了 wley 用户，其 NTLMv2 哈希是我们在此次评估早期通过运行 Responder 获取的
该用户使用了弱密码，能够使用 Hashcat 离线破解哈希并检索出其明文值
可以利用此访问权限发起一个攻击链 (attack chain)，最终控制能够执行 DCSync 攻击的 adunn 用户
这将能够检索域中所有用户的 NTLM 密码哈希，并将权限提升 (escalate privileges) 到域/企业管理员 (Domain/Enterprise Admin) 实现持久化 (persistence)，完全控制该域
执行以下操作：
            使用 wley 用户更改 damundsen 用户的密码
            以 damundsen 用户身份进行身份验证，并利用 GenericWrite 权限将我们控制的用户添加到 Help Desk Level 1 组
            利用 Information Technology 组中的嵌套组员身份，并利用 GenericAll 权限来控制 adunn 用户


